<% lists.each do |l| %>
  <div class="card-category m-2" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path l.photo.key, height: 300, crop: :fill %>')">
    <%= link_to l.name, list_path(l) %>
  </div>
<% end %>



